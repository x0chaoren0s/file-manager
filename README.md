# File Manager (文件管理器)

这是一个现代化的、模块化的 Web 浏览器端文件管理器。它基于 Vite 构建，支持模块化开发，并能生成单一的 HTML 文件以便于部署。

## ✨ 核心功能

### 📁 文件管理
- **WebDAV 支持**：完整支持 PROPFIND, PUT, MOVE, DELETE 方法
- **降级兼容**：当 WebDAV 不可用时，自动尝试解析标准 Nginx/Python 的 HTML 目录索引
- **拖拽上传**：支持拖拽文件到浏览器直接上传
- **批量操作**：支持文件删除、下载等操作

### 👁️ 文件预览
- **代码预览**：支持 30+ 种编程语言的语法高亮（Highlight.js + GitHub Dark 主题）
- **Markdown 渲染**：支持 GFM 语法、代码块高亮及 MathJax 公式
- **图片预览**：支持 PNG, JPG, GIF, SVG, WebP 等主流图片格式
- **视频在线播放**：支持 MP4, WebM, OGG, MOV 等视频格式在线播放
- **行号显示**：默认开启行号，防止复制时携带行号

### 🧩 代码折叠
- **大括号语法**：自动识别 `{}` 代码块（适用于 JS, JSON, CSS, Java, C 等）
- **缩进语法**：自动识别 Python 的 `def`, `class`, `if`, `for` 等缩进块
- **交互式折叠**：点击行号区域的 ▶ 箭头或折叠后的 `...` 占位符即可展开/折叠

### 🚀 快速部署
- **一键部署脚本**：`deploy.sh` 自动配置 Nginx，支持自定义域名、端口、子路径
- **HTTPS 支持**：自动扫描系统证书并启用 SSL
- **宝塔面板兼容**：深度适配宝塔面板 (BT Panel) 的目录结构

## 为什么选择本项目？ (对比 Jupyter Notebook/Lab)

虽然 Jupyter 系列提供了强大的交互能力，但本项目在以下场景更具优势：

| 特性 | 本项目 (Minimal File Manager) | Jupyter Notebook / Lab |
| :--- | :--- | :--- |
| **资源开销** | **极低**。单 HTML 文件 (~29KB)，Nginx 驱动，近乎零内存/CPU 占用。 | **较高**。需要 Python 后台及大量依赖，内存占用从几百 MB 起步。 |
| **标准协议** | **WebDAV**。可作为网络硬盘直接挂载到 Windows/macOS/手机。 | **专有协议**。主要通过 Web 界面或专有 API 交互。 |
| **部署难度** | **极简**。一键脚本，自动扫描证书，秒级部署。 | **普通**。需要配置内核、子路径转发及复杂的环境隔离。 |
| **安全性** | **高**。仅限文件操作，攻击面小，无远程代码执行风险。 | **一般/有风险**。具备 RCE 能力，凭据泄漏可能导致整机沦陷。 |
| **阅读体验** | **专注**。专业编辑器风格预览窗，代码高亮、行号、折叠一应俱全。 | **综合**。界面复杂，包含大量内核状态与执行控件。 |

> 如果您只需要一个轻量、标准、安全且能快速"看一眼"和"传文件"的工具，本项目是更清爽的选择。

## 开发与构建

项目使用 Node.js (推荐 v24+) 及 Vite 进行开发。

### 1. 安装依赖
```bash
npm install
```

### 2. 开发模式
```bash
npm run dev
```

### 3. 构建单文件
```bash
npm run build
```
产物将生成在 `dist/index.html`。

## 生产部署

您可以直接使用内置的部署脚本：

```bash
chmod +x deploy.sh
sudo ./deploy.sh
```

按照提示输入域名、是否开启 HTTPS、子路径等信息，脚本将自动配置 Nginx 并迁移文件。

## 技术栈

- **构建工具**: Vite + vite-plugin-singlefile
- **前端语言**: Vanilla JavaScript (ES6+)
- **样式**: Vanilla CSS
- **代码高亮**: [Highlight.js](https://highlightjs.org/) (GitHub Dark 主题)
- **Markdown 引擎**: [marked](https://github.com/markedjs/marked) + [DOMPurify](https://github.com/cure53/dompurify)
- **公式渲染**: [MathJax](https://www.mathjax.org/)

## 许可说明

本项目优先使用 WebDAV 方法，建议在 Nginx 环境下配合 `nginx-extras` 使用以获得最佳体验。
